# 🚀 Руководство по развертыванию для заказчика

## 📋 Системные требования

### Минимальные требования:
- **ОС**: Windows 10/11, Linux (Ubuntu 20.04+), macOS 10.15+
- **Python**: 3.9 или выше
- **RAM**: 16GB (рекомендуется 32GB для 50k+ документов)
- **Диск**: 50GB свободного места (рекомендуется SSD)
- **GPU**: NVIDIA с 8GB+ памяти (рекомендуется RTX 3080/4080)

### Рекомендуемые требования:
- **RAM**: 32GB или больше
- **Диск**: 100GB+ на SSD
- **GPU**: NVIDIA RTX 3080/4080/4090 с 12GB+ памяти
- **Сеть**: Стабильное интернет-соединение для OpenAI API

## 🔧 Быстрая установка (рекомендуется)

### Шаг 1: Подготовка
1. Скачайте проект и распакуйте в удобное место
2. Откройте командную строку в папке проекта
3. Убедитесь, что Python 3.9+ установлен

### Шаг 2: Автоматическая установка
```bash
python quick_start.py
```

Этот скрипт автоматически:
- ✅ Проверит системные требования
- ✅ Создаст виртуальное окружение
- ✅ Установит все зависимости
- ✅ Настроит CUDA (если доступна)
- ✅ Протестирует установку

### Шаг 3: Настройка конфигурации
1. Скопируйте `env.example` в `.env`
2. Отредактируйте `.env` файл:
   ```bash
   # Обязательно:
   OPENAI_API_KEY=sk-your-openai-api-key-here
   
   # Для больших объемов (50k+ документов):
   MAX_DOCS_PER_BATCH=100
   BATCH_SIZE=1
   EPOCHS=10
   ```

## 📄 Подготовка данных

### Структура папок:
```
data/
├── raw/           # Поместите сюда PDF документы
├── processed/     # Обработанные документы (создается автоматически)
├── analyzed/      # Результаты анализа (создается автоматически)
└── structured/    # Структурированные данные (создается автоматически)
```

### Подготовка PDF документов:
1. Поместите все PDF файлы в папку `data/raw/`
2. Убедитесь, что файлы читаемы (не повреждены)
3. Рекомендуется использовать SSD для быстрой обработки

## 🚀 Запуск обработки

### Для небольших объемов (до 1000 документов):
```bash
# Активируйте окружение
.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/Mac

# Запустите полный пайплайн
python run_pipeline.py --epochs 10 --batch-size 1
```

### Для больших объемов (50,000+ документов):
```bash
# Активируйте окружение
.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/Mac

# Запустите оптимизированный пайплайн
python run_large_scale.py --input-dir data/raw --batch-size 100 --epochs 10
```

## 📊 Мониторинг процесса

### Просмотр логов:
```bash
# Основные логи
tail -f logs/app.log

# Логи для больших объемов
tail -f logs/large_scale.log
```

### Мониторинг через TensorBoard:
```bash
python src/monitor_training.py --log_dir models --port 6006 --open_browser
```

### Проверка прогресса:
```bash
# Количество обработанных документов
ls data/processed/ | wc -l

# Количество проанализированных документов
ls data/analyzed/ | wc -l
```

## 🖥️ Использование GUI

После завершения обучения:
```bash
python gui/app.py
```

GUI позволяет:
- 📝 Вводить фактические обстоятельства дела
- 🎯 Генерировать мотивировочную часть
- 💾 Сохранять результаты
- 📊 Просматривать историю генераций

## ⚠️ Важные замечания

### Время обработки (примерные оценки):
- **1000 документов**: 2-4 часа
- **10,000 документов**: 1-2 дня
- **50,000 документов**: 5-10 дней
- **100,000 документов**: 10-20 дней

### Оптимизация производительности:
1. **Используйте SSD** для быстрого доступа к данным
2. **Закройте другие программы** во время обработки
3. **Используйте GPU** для ускорения обучения
4. **Настройте batch_size** в зависимости от доступной памяти

### Устранение неполадок:

#### Ошибка CUDA:
```bash
python check_cuda.py
```

#### Проверка зависимостей:
```bash
python quick_test.py
```

#### Тест OpenAI API:
```bash
python src/test_openai_key.py
```

## 📞 Поддержка

### Логи для диагностики:
- `logs/app.log` - основные логи приложения
- `logs/large_scale.log` - логи для больших объемов
- `logs/training.log` - логи обучения

### Полезные команды:
```bash
# Проверка системы
python check_cuda.py

# Быстрый тест
python quick_test.py

# Очистка данных
python clean_data.py

# Управление моделями
python src/model_manager.py list
```

## 🎯 Рекомендации для 50,000+ документов

1. **Используйте пакетную обработку**:
   ```bash
   python run_large_scale.py --mode batch --batch-size 100
   ```

2. **Мониторьте ресурсы**:
   - Следите за использованием RAM
   - Контролируйте температуру GPU
   - Проверяйте свободное место на диске

3. **Делайте резервные копии**:
   ```bash
   python src/model_manager.py backup models/legal_model
   ```

4. **Используйте инкрементальное обучение**:
   ```bash
   python src/incremental_train.py --model_path models/legal_model --new_data data/new_example.jsonl
   ```

## ✅ Чек-лист готовности

- [ ] Python 3.9+ установлен
- [ ] Виртуальное окружение создано
- [ ] Все зависимости установлены
- [ ] CUDA работает (если есть GPU)
- [ ] OpenAI API ключ настроен
- [ ] PDF документы помещены в `data/raw/`
- [ ] Достаточно места на диске (50GB+)
- [ ] Достаточно RAM (16GB+)
- [ ] Стабильное интернет-соединение

После выполнения всех пунктов система готова к обработке 50,000+ документов! 